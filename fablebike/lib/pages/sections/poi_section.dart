class POISection extends StatefulWidget {
  POISection({Key key}) : super(key: key);

  @override
  _POISectionState createState() => _POISectionState();
}

class _POISectionState extends State<POISection> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}
